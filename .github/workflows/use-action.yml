name: Use actions Workflow 
on: push
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code // clone the repository
        uses: actions/checkout@v4
      - name: List files in the repository
        run: ls